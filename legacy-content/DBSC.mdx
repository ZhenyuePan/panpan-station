---
title: "DBSC笔记"
publishedAt: "2024-10-30"
summary: "a note for DBSC ＆ 15445"
---

# a note for DBSC
## 第一章 引言

DBMS：数据库管理系统

DDL：数据定义语言

DML：数据操纵语言（manipulate）

ACID：

CRUD：增删改查

视图：

数据模型

* 关系模型：关系模型用表的集合来表示数据和数据间的关系
* 实体-联系模型：E-R数据模型使用称作实体的基本对象的集合，以及这些对象间的联系。
* 半结构化数据模型
* 基于对象的数据模型

数据抽象

* 物理层
* 逻辑层
* 视图层

实例：特定时刻存储在数据库中的信息称作数据库的一个实例

## 第二章 关系模型介绍

关系数据库由表的集合构成

在关系模型中，术语**关系**被用来指代表，而术语**元组**被用来指代行。类似地，术语**属性**指代的是表中的列

域：每个属性允许取值的集合

空值：一个特殊的值，它表示值未知或者并不存在

数据库模式：数据库的逻辑设计

数据库实例：给定时刻数据库中数据的一个快照

**一个关系中不可以有两个元组在所有属性取值上完全相同**

**超码**：一个或多个属性的集合

**主码**：代表数据库设计者选中来作为在一个关系中区分不同元组的主要方式的候选码

**外码**（外码约束）：

**引用完整性约束**要求引用关系中的任意元组在指定属性上出现的取值也必然出现在被引用关系至少一个元组的指定属性上。

查询语言：query language

命令式查询语言：

函数式查询语言：

* 关系代数
* 元组关系演算和域关系演算







